// Crie um programa que exiba os números ímpares de 1 a 50 usando o loop "while".

let contadora = 1;

while (contadora <= 50) {
  if (contadora % 2 !== 0) {
    console.log(contadora);
  }
  contadora++;
}
